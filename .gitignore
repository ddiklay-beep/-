# Claude Code local settings (per-machine, not for the repo)
.claude/settings.local.json

# Local environment / secrets — never commit
.env
.env.local
.env.*.local

# Generated images — keep .gitkeep + sibling .txt prompts; don't bloat git with PNGs
yuval/outputs/*.png
Output/images/*.png
